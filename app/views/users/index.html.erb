<% provide(:title, 'All users') %>
<h1>All users</h1>

<div class="digg_pagination">
  <%= will_paginate %>
</div>

<ul class="users">
  <%= render @users %> <!-- Here Rails infers that @users is a list of User objects; moreover, when called with a collection of users, Rails automatically iterates through them and renders each one with the _user.html.erb partial -->
</ul>

<div class="digg_pagination">
  <%= will_paginate %>
</div>
